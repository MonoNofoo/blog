import { style } from '@vanilla-extract/css';

export const pageTitle = style({
  margin: '24px 0 0',
});

export const articleList = style({
  marginTop: '24px',
  display: 'grid',
  gap: '16px',
});
